


#
#	how to publish:
#
#		python3 status/status.py
#		(rm -rf dist && python3 -m build && twine upload dist/*)
#		(git add --all && git commit -m "botanical" && git push --set-upstream git@gitlab.com:reptilian_climates/modules_series_3/botanical.git climate)
#

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "botanical"
version = "1.0.0"
dependencies = [
    'psutil'
]

license = { text = "GPL 3.0" }
# license = { file = "" }

description = "various botanical tools"
readme = "module.txt"



[tool.setuptools.packages.find]
where = [ "fields/gardens" ]

#
#	https://setuptools.pypa.io/en/latest/userguide/datafiles.html
#
[tool.setuptools.package-data]
"*" = [ "*.html" ]