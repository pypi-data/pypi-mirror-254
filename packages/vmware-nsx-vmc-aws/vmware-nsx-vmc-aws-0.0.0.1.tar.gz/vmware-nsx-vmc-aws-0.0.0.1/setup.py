from setuptools import setup, find_packages

setup(
    name="vmware-nsx-vmc-aws",
    version="0.0.0.1",
    author="VMware Inc.",
    description="Python bindings for nsx vmc aws",
    packages=find_packages(),
    install_requires=[
        # List your package dependencies here
    ],
)

