$ lsrec

  lsrec is an example application that implements some
  multi commands that chain image processing instructions
  together.

  This requires pillow.

Usage:

  $ pip install --editable .
  $ lsrec open -i example01.jpg resize -w 128 display
  $ lsrec open -i example02.jpg blur save
