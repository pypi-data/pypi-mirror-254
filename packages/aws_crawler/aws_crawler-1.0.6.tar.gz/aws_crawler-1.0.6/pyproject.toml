[tool.poetry]
name = "aws_crawler"
version = "1.0.6"
description = "Crawl through active AWS accounts in an organization using master assumed role."
readme = "README.rst"
authors = ["Ahmad Ferdaus Abd Razak <ahmad.ferdaus.abd.razak@ni.com>"]
license = "GPL-2.0-only"
repository = "https://gitlab.com/fer1035_python/modules/pypi-aws_crawler"
keywords = ["AWS", "crawler", "accounts", "organization"]

[tool.poetry.dependencies]
python = "^3.9"
boto3 = "^1.17.78"
aws-authenticator = "^2022.10.1.3"
multithreader = "^1.0.1"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
