[tool.poetry]
name = "z-test-poetry"
version = "0.1.0"
description = ""
authors = ["Ermakov V.N. <v.n.ermakov@gmail.com>"]
readme = "README.md"
packages = [{include = "z_test_poetry", from = "src"}]

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.31.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
