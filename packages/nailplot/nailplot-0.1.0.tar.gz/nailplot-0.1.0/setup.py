# setup.py

from setuptools import setup

setup(
    name='nailplot',
    version='0.1.0',
    packages=[''],  # Ensure this is an empty list
    install_requires=['requests'],  # Add any other dependencies here
)
