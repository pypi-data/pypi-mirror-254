[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"


[project]
name = "fastapi-services"
version = "1.0.0"

[tool.setuptools.packages.find]
exclude = ["fastapi-services.tests*", "example*"]


