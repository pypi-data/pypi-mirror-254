

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "Coccinellidae"
version = "0.0.3"
dependencies = []

[tool.setuptools.packages.find]
where = [ "structures/modules" ]