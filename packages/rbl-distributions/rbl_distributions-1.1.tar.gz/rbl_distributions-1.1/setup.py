from setuptools import setup

setup(name='rbl_distributions',
      version='1.1',
      description='Gaussian and Binomial distributions',
      packages=['rbl_distributions'],
      author='Ochieng Billy',
      zip_safe=False)
