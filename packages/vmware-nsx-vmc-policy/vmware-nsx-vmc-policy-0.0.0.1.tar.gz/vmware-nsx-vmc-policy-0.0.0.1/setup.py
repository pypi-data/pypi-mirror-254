from setuptools import setup, find_packages

setup(
    name="vmware-nsx-vmc-policy",
    version="0.0.0.1",
    author="VMware Inc.",
    description="Python bindings for nsx vmc policy",
    packages=find_packages(),
    install_requires=[
        # List your package dependencies here
    ],
)

