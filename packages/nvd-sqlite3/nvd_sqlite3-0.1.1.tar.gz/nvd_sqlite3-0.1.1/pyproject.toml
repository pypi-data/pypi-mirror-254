[tool.poetry]
name = "nvd-sqlite3"
version = "0.1.1"
description = ""
authors = ["Cristiano Henrique <cristianovisk@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.31.0"
pandas = "^2.2.0"
pyarrow = "^15.0.0"

[tool.poetry.scripts]
process_nvd = "nvd_sqlite3.process:run"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
