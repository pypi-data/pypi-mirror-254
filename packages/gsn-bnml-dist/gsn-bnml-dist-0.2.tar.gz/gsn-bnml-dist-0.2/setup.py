from setuptools import setup

setup(name='gsn-bnml-dist',
      version='0.2',
      description='Gaussian and binomial distributions',
      packages=['gsn-bnml-dist'],
      author='Shaun Bishop',
      zip_safe=False)
