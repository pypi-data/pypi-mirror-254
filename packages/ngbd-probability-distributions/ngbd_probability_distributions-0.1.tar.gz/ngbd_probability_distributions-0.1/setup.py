from setuptools import setup

setup(name='ngbd_probability_distributions',
      version='0.1',
      description='Normal, Gaussian, Binomial distributions',
      packages=['ngbd_probability_distributions'],
      zip_safe=False)
