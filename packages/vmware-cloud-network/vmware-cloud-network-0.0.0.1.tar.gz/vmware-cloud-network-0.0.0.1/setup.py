from setuptools import setup, find_packages

setup(
    name="vmware-cloud-network",
    version="0.0.0.1",
    author="VMware Inc.",
    description="Python bindings for vmwarecloud network",
    packages=find_packages(),
    install_requires=[
        # List your package dependencies here
    ],
)

