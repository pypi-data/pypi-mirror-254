from setuptools import setup, find_packages

setup(
    name='datareader-add_fields',
    version='0.1.0',
    author="Calyx",
    packages=find_packages(),
    install_requires=[
        # Lista de dependencias si las tienes
    ],
)