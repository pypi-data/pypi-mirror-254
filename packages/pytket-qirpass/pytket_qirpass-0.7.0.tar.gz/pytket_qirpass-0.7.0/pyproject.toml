[build-system]
requires = ["flit_core >=3.9.0,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "pytket-qirpass"
authors = [{name = "Alec Edgington", email = "alec.edgington@quantinuum.com"}]
readme = "README.md"
license = {file = "LICENSE"}
classifiers = ["License :: OSI Approved :: Apache Software License"]
dynamic = ["version", "description"]
dependencies = ["llvmlite >= 0.42.0,<0.43", "pytket >= 1.24.0,<2"]

[project.urls]
Home = "https://github.com/CQCL/pytket-qirpass"

[project.optional-dependencies]
tests = ["pyqir >=0.10.0,<0.11"]
