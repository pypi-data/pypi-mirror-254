[project]
name = "toolkey"
version = "1.1.2"
authors = [
  { name="PyKeys", email="py-keys@hotmail.com" },
]
description = " check if imported or if number name"
readme = "README.md"

classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

[project.urls]
Issues = "https://github.com/PyKeys/impo/issues"
Twitter = "https://twitter.com/py_keys"
Wordpress = "https://pykeys.wordpress.com"


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
