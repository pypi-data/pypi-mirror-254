from setuptools import setup

setup(name='distributions_renatosfmedeiros',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_renatosfmedeiros'],
      zip_safe=False)
