[tool.poetry]
name = "py_entity_analytics"
version = "0.0.1"
description = "Toolkit for Agolo's Entity Analytics ... in Python!"
authors = ["Muhammad Nashaat <muhammad.nashaat@agolo.com>"]
readme = "README.md"
packages = [

]

[tool.poetry.dependencies]
python = "^3.10"
requests = "2.31.0"
tabulate = "0.9.0"

[tool.poetry.group.test.dependencies]
pytest = "^8.0.0"

[build-system]
requires = ["poetry-core>=1.0.8"]
build-backend = "poetry.core.masonry.api"