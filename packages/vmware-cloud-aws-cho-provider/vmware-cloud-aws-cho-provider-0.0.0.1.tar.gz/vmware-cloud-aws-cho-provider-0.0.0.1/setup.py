from setuptools import setup, find_packages

setup(
    name="vmware-cloud-aws-cho-provider",
    version="0.0.0.1",
    author="VMware Inc.",
    description="Python bindings for vmwarecloud aws cho provider",
    packages=find_packages(),
    install_requires=[
        # List your package dependencies here
    ],
)

